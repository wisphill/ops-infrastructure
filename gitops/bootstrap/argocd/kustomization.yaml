apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-role-binding.yaml
  - service-account.yaml
  - helm-app.yaml
  - cmp.yaml

# generators:
#   - secrets.enc.yaml
